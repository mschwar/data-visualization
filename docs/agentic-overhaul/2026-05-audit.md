# Agentic Overhaul Audit: 2026-05

Audit date: 2026-05-19

## Repo Inventory

Repo type: static site / visualization showcase

Primary files:

- `README.md`: public overview, live site link, publication context
- `index.md`: GitHub Pages page content for the visualization
- `_config.yml`: Jekyll/Minima configuration
- `assets/css/style.scss.css`: site styling overrides
- `oral-health-heatmap.png`: core visualization image
- `README.CRAWL.md`: historical directory crawl artifact, not canonical documentation

Infrastructure added in this audit:

- `.gitignore`
- `scripts/validate_repo.py`
- `AGENTS.md`
- `CURRENT_STATE.md`
- `.github/workflows/validate.yml`

Removed in this audit:

- `.DS_Store`
- `assets/.DS_Store`

## Maturity Score

Scale: 0 = absent, 1 = fragile, 2 = basic, 3 = workable, 4 = strong, 5 = excellent

| Category | Score | Notes |
| --- | --- | --- |
| Docs | 2 | Core story exists, but operational/project docs were minimal before this pass. |
| Build | 1 | GitHub Pages intent is clear, but no pinned local Jekyll toolchain is committed. |
| Tests | 1 | No test framework; now has a basic structural validator only. |
| Data / provenance | 1 | Publication citation exists, but source data and generation pipeline are absent. |
| CI | 0 | No workflow existed before this audit. |
| Accessibility / performance | 2 | Main image has alt text and responsive CSS; no automated checks. |
| Deployment | 2 | GitHub Pages destination is clear, but deployment config is implicit rather than documented. |
| Maintainability | 2 | Small repo is easy to inspect, but lacked orientation and validation guidance. |

## P0 Backlog

- Decide whether to pin a local Jekyll toolchain or explicitly document that local build is unsupported/unverified.
- Document or add the source-of-truth artifact used to generate `oral-health-heatmap.png` if it exists.
- Verify that the live GitHub Pages site still reflects the committed repository contents.

## Resolution Notes

- Local build tooling is now pinned with `Gemfile` and `.ruby-version`, and the build is wired into CI.
- The heatmap source artifact is now committed as `Figure2-Teeth_v4.1.pdf`, with `scripts/render_heatmap.py` and `docs/heatmap-provenance.md` documenting the rebuild path.
- The live GitHub Pages site returned `200 OK` and served the expected heatmap page in this session.

## P1 Backlog

- Add a provenance note describing what inputs and scripts produced the final heatmap, if those materials can be published safely.
- Add a screenshot or rendered-page check in CI if a stable local build path is introduced.
- Clarify whether `README.CRAWL.md` should remain as an artifact, move under `docs/`, or be removed.

## P2 Backlog

- Consider splitting narrative content into reusable includes only if the site grows beyond a single page.
- Consider adding front matter defaults or richer metadata if the site expands into a portfolio.
- Add a more explicit accessibility review once there are more images, charts, or interactive elements.

## Do Not Do Yet

- Do not introduce a frontend framework or static-site generator replacement.
- Do not invent a reproducible data pipeline unless the original source materials are available.
- Do not add heavy lint/test infrastructure to a one-page site without a clear maintenance payoff.
- Do not add deployment automation beyond validation unless GitHub Pages behavior needs correction.
